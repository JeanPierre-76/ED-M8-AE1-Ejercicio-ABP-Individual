<p style="color: green"><%= notice %></p>

<h1>Contactos</h1>

<div id="contacts">
  <% @contacts.each do |contact| %>
    <%= render contact %>
    <p>
      <%= link_to "Mostrar este contacto", contact %>
    </p>
  <% end %>
</div>

<%= link_to "Nuevo contacto", new_contact_path %>
