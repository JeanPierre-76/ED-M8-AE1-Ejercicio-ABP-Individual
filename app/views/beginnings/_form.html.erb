<%= form_with(model: beginning) do |form| %>
  <% if beginning.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(beginning.errors.count, "error") %> prohibited this beginning from being saved:</h2>

      <ul>
        <% beginning.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_area :name %>
  </div>

  <div>
    <%= form.label :correo, style: "display: block" %>
    <%= form.text_area :correo %>
  </div>

  <div>
    <%= form.label :descripcion_de_tu_perfil_hobbies_y_pasiones, style: "display: block" %>
    <%= form.text_area :descripcion_de_tu_perfil_hobbies_y_pasiones %>
  </div>

  <div>
    <%= form.label :ultimos_dos_trabajos_o_estudios, style: "display: block" %>
    <%= form.text_area :ultimos_dos_trabajos_o_estudios %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
